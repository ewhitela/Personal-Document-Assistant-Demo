# Python
__pycache__/
*.pyc

# Project data (generated at runtime)
pdva_docs/
pdva_store/

# Model weights: downloaded during setup, not committed.
# jarona.onnx is the exception — it is our own trained wake word.
*.onnx
!jarona.onnx
*.onnx.json
*.tflite
!jarona.tflite

# Audio scratch. tests/data/sample.wav is a fixture, keep it.
*.wav
!tests/data/sample.wav

# OS / editor cruft
.DS_Store
*.save.venv/
*.ipynb
.venv/
scratch/
